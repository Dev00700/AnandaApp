var _userId = "";$(document).ready(function () {    $(document).on("change", "#dropdown-select", (function () {        dropDownList();    }));});$('#selectAllIsActive').on('change', function () {    let isChecked = $(this).is(':checked');    $('#tblBody input[id^="isactive_"]').each(function () {        $(this).prop('checked', isChecked);    });});function dropDownList() {    debugger;    $("#chkSelect").prop('checked', false);    let _userId = $('#dropdown-select option:selected').val();    $.ajax({        url: 'GetMenus',        type: 'POST',        data: { userId: _userId },        success: function (data) {            var row = '';            var count = 1;            if (data.length > 0) {                let row = "";                for (let i = 0; i < data.length; i++) {                    row += `<tr class="gradeX" id="trRow_${i + 1}">`;                    row += `<td>${i + 1}</td>`;                    //for Select all                    row += `<td class="text-center">    <input type="checkbox"           class="checkbox"           id="isactive_${i + 1}"             ${data[i].isActive ? 'checked' : ""} /></td>`;                    row += `<td>${data[i].menuCode}</td>`;                    row += `<td>${data[i].menuName}</td>`;                    row += `            <td class="text-center">                <input type="checkbox"                        class="checkbox"                        id="chkAdd_${i + 1}"                        ${data[i].forAdd ? 'checked' : ""} />            </td>`;                    row += `            <td class="text-center">                <input type="checkbox"                        class="checkbox"                        id="chkEdit_${i + 1}"                        ${data[i].forEdit ? 'checked' : ""} />            </td>`;                    row += `            <td>                <input type="hidden"                        id="txtmenuId_${i + 1}"                        value="${data[i].menuId}"/>            </td>`;                    row += `            <td>                <input type="hidden"                        id="txtusermenuId_${i + 1}"                        value="${data[i].userMenuId}"/>            </td>`;                    row += `            <td>                <input type="hidden"                        id="txtusermenuGuid_${i + 1}"                        value="${data[i].userMenuGuid}"/>            </td>`;                    row += `</tr>`;                }                $('#tblBody').empty();                $('#tblBody').html(row);            }            else {                $('#tblBody').empty();                ShowErrorMessage("Error")            }        },    });}function Validation() {    debugger;    let _userId = $('#dropdown-select option:selected').val();    if (_userId == null || _userId == 0) {        WarningMsg('User can not be left blank.');        return false;    }    else {        saveMenuSettings();    }}function saveMenuSettings() {    debugger;    let menuData = [];    $('#tblBody tr').each(function () {        let row = $(this);        let userId = $('#dropdown-select option:selected').val();        let menuId = row.find('input[id^="txtmenuId_"]').val();        let forAdd = row.find('input[id^="chkAdd_"]').is(':checked');        let forEdit = row.find('input[id^="chkEdit_"]').is(':checked');        let userMenuId = row.find('input[id^="txtusermenuId_"]').val();        let userMenuGuid = row.find('input[id^="txtusermenuGuid_"]').val();        let isActive = row.find('input[id^="isactive_"]').is(':checked');        if (isActive) {            let existingEntry = menuData.find(item => item.MenuId === menuId);            if (existingEntry) {                existingEntry.ForAdd = forAdd;                existingEntry.ForEdit = forEdit;                existingEntry.UserMenuId = userMenuId;                existingEntry.UserMenuGuid = userMenuGuid;                existingEntry.IsActive = isActive;            } else {                menuData.push({                    MenuId: menuId,                    UserId: userId,                    ForAdd: forAdd,                    ForEdit: forEdit,                    UserMenuId: userMenuId,                    UserMenuGuid: userMenuGuid,                    IsActive: isActive                });            }        }    });    $.ajax({        url: 'Save',        type: 'POST',        contentType: 'application/json',        data: JSON.stringify(menuData),        success: function (response) {            if (response.success) {                SuccessMsg("Menus Assigned successfully!");            } else {                ErrorMsg("Failed to Assign Menus");            }        },        error: function () {            ErrorMsg("An error occurred while saving settings.");        }    });    window.location.href = "/UserMenuAuthorization/index";}